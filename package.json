{
  "name": "agar.io",
  "description": "Agar.io clone.",
  "main": "build/server/index.js",
  "contributors": [
    "Ben Budnevich <ben.bj32001@gmail.com>",
    "Mark <mark@remarkablemark.org>"
  ],
  "scripts": {
    "build:client": "tsc -p src/client",
    "build:server": "tsc -p src/server",
    "build:watch:client": "yarn build:client --watch",
    "build:watch:server": "yarn build:server --watch",
    "server": "nodemon build/server",
    "start": "npm-run-all --parallel build:watch:client build:watch:server server"
  },
  "dependencies": {
    "express": "latest",
    "nodemon": "latest",
    "randomcolor": "latest",
    "socket.io": "latest"
  },
  "devDependencies": {
    "@commitlint/cli": "^8.2.0",
    "@commitlint/config-conventional": "^8.2.0",
    "husky": "^3.0.8",
    "npm-run-all": "^4.1.5",
    "typescript": "^3.6.3"
  },
  "license": "MIT"
}
